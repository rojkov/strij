syntax = "proto3";

package strij.config;

import "google/protobuf/descriptor.proto";

// Shared logging configuration message
message Logging {
  string level = 1 [(strij.config.enum_values) = "trace",
                    (strij.config.enum_values) = "debug",
                    (strij.config.enum_values) = "info",
                    (strij.config.enum_values) = "warn",
                    (strij.config.enum_values) = "error"];
  string format = 2 [(strij.config.enum_values) = "text",
                     (strij.config.enum_values) = "json"];
  string output = 3 [(strij.config.enum_values) = "stdout",
                     (strij.config.enum_values) = "stderr"];
  // RESERVED for future: string file_path = 4;  // Required if output="file" (future)
  bool include_source_location = 5;
}

// Custom validation options for protobuf config fields.
// Usage:
//   uint32 port = 1 [(strij.config.required) = true,
//                    (strij.config.range_min) = "1",
//                    (strij.config.range_max) = "65535"];
extend google.protobuf.FieldOptions {
  bool required = 50000;
  string range_min = 50001;
  string range_max = 50002;
  repeated string enum_values = 50003;
  string pattern = 50004;
  string deprecated_message = 50005;
}
