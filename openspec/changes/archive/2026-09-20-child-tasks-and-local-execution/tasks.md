## 1. Node foundation (4A): child submission seam

- [x] 1.1 Introduce `repeated NodeSchedulerConfig { ExtensionConfig extension; string task_type; bool local_default; }` in `api/nodeagent/config/nodeagent.proto` (node-side shape, distinct from the gateway `SchedulerConfig`); update the config loader/validation to reject empty lists, unknown names, duplicate non-empty `task_type`s, more than one `local_default`, and to treat an empty `task_type` as NO local role (not a default).
- [x] 1.2 Implement `LocalResultReceiverStorage` (node-side mirror of `gateway::ResultReceiverStorage`): `Put(task_id, ReceiverPtr)`, `Get`, `Erase`, `Empty`, `Size`; **owned privately by the bundled `default` scheduler** (with a test-facing `Storage()` accessor), in `src/nodeagent/core/`.
- [x] 1.3 Implement `StorageResultSender` (a `nodeagent::ResultSender` bound to the storage + fixed `task_id`): `Send` resolves the receiver by id, delivers body/finality, erases on final; lifecycle hooks supported.
- [x] 1.4 Add the sender-backed `RunTaskService` overload `RunTask(const Task&, ResultSenderPtr, AdmissionScopePtr reserved)` (preallocated); keep the existing `io::Connection`-bound overloads as thin wrappers delegating to the shared path.
- [x] 1.5 Implement the bundled `default` local scheduler (`DefaultLocalScheduler`, factory name `"default"`, empty `DefaultSchedulerConfig`, no `RequiredProtocol`): its `Schedule` is the single child-policy step — register the receiver in its own storage first, then `AdmissionController::Admit` → on success run via sender-backed `RunTask` with a `StorageResultSender`; on failure forward via `ChildTaskForwarder` (4B) or, when forwarding is unavailable/unreachable, deliver an error and erase the entry (the parent never hangs).
- [x] 1.6 Implement `NodeagentSchedulerRouter` (node-side `extensions::Scheduler` composite): `Schedule` dispatches by `child.type()` to the entry declaring that `task_type`, falls back to the single `local_default` entry, and delivers an error when nothing claims the type and no local default is declared; entries with no local role never receive submissions. **The router is also the node's frame demux**: `HandledFrameTypes()` is the dedup union of constituents and `HandleFrame` dispatches by `type_id` via `findFrameOwner` (NotFound when unclaimed), mirroring the gateway `SchedulerRouter`.
- [x] 1.7 Make `PushLocalScheduler::Schedule` and `ProbeLocalScheduler::Schedule` **Unimplemented** (log a warning + `DeliverError`, honoring the resolve contract): the `default` scheduler is the single local authority; `probe` never queues a child (the probe dance is pointless for a local child).
- [x] 1.8 Define the narrow `ChildTaskSubmitter` interface (`Submit(const Task&, gateway::ResultReceiverPtr)`) as the node-global shared instance wrapping the `NodeagentSchedulerRouter`; add `ChildTaskSubmitter()` accessor to `NodeagentFactoryContext` (via `SetChildTaskSubmitter` two-phase install in the impl).
- [x] 1.9 Give the **`default` scheduler** the child-outcome frame ownership: `HandledFrameTypes()` = `{kResult, kTaskRejected}`; `HandleFrame(kResult)` resolves the id in its storage (`Deliver`, final erases), `HandleFrame(kTaskRejected)` delivers the reason error and erases; unknown ids dropped with a warning. Rework `NodeagentTlvHandler` into a thin seam — core `kNodeAdvertisement` write plus a default-case that routes every other frame to the router (copying `gateway_tlv_handler.cc`'s seam); non-Ok router statuses drop with a warning.
- [x] 1.10 Add the example workflow task handler (`"workflow"` factory in `Registry<TaskHandlerFactory>`): factory retrieves `context.ChildTaskSubmitter()`, passes to handler ctor; handler fans out to child tasks with `GenerateTaskId()` ids, aggregates final bodies into the parent result, and aborts on a child error.
- [x] 1.11 Wire `nodeagent_framework.cc`: build `GatewayClient` + install as `ChildTaskForwarder` before schedulers, build schedulers (applying the `task_type`/`local_default` role declarations), build `NodeagentSchedulerRouter` (+ submitter), pass the router to every `NodeagentTlvHandler`, register accepted connections with the `GatewayClient`.
- [x] 1.12 Tests for 4A: storage put/get/erase; `StorageResultSender` delivery; the sender-backed `RunTask` preallocated path (preallocated-release, no-handler drop); `NodeagentSchedulerRouter` (type claim, explicit local-default fallback, no-claim-without-default error, no-role entry never scheduled, owned frame union, unclaimed frame NotFound); `default` scheduler (admit-run-local, unhandled/unadmitted → error + erase, forward keeps entry until result frame, failed forward → error + erase, local-beats-forward, result/rejected frame resolution incl. non-final and unknown id, empty `RequiredProtocol`); push/probe `Schedule` Unimplemented; handler seam (routing kResult through the router to the default scheduler, no-owner frame dropped with warning, no-router handler drops everything); config schema + role validation (duplicate `task_type`, multiple `local_default`, empty `task_type` ≠ default); workflow handler fan-out/aggregate/abort; `echo`/`piped_executable` handlers and their tests unchanged (verification of the locked interface decision).

## 2. Forward path (4B): GatewayClient

- [x] 2.1 Add `NodeAgentConfig.gateway_client { repeated string addresses; }` to the proto; optional section, valid-endpoint validation.
- [x] 2.2 Implement `GatewayClient` (exposed via `NodeagentFactoryContext`): a `ChildTaskForwarder` whose `Forward(Task)` writes an upstream `kTaskSubmission` frame; `RegisterConnection(mailbox)` lets the node's accepted connections register with it; FIFO round-robin over live connections; error (non-Ok `Forward`) when nothing is reachable.
- [x] 2.3 Wire `nodeagent_framework.cc`: create the `GatewayClient`, register each accepted connection on accept, install into the context (before schedulers with a forward path are built).
- [x] 2.4 Tests: write-on-live-connection; round-robin across N live connections; no live connection → non-Ok `Forward` (parent error via the default scheduler's forward path).

## 3. Gateway inbound (4B): upstream child routing

- [x] 3.1 Implement `NodeConnectionResultReceiver` (TLV sibling of `HttpResultReceiver`): `Deliver`/`DeliverError` write `kResult`/`kTaskRejected` frames on the connection's mailbox.
- [x] 3.2 Teach `SchedulerRouter` to claim `kTaskSubmission` (`HandledFrameTypes()` + `HandleFrame`): parse `task::Task`, build + store a `NodeConnectionResultReceiver` keyed by task id and submitting node id, dispatch through normal `Schedule` routing; unmatched-with-no-default → `DeliverError`.
- [x] 3.3 Verify `GatewayTlvHandler`/`gateway_framework.cc` require no new wiring (default seam already routes to the router; `NotifyNodeDisconnected` already cleans up by node id); adjust if the seam needs a status contract tweak for claimed types.
- [x] 3.4 Tests: upstream submission routes by type; unmatched upstream child writes `kTaskRejected` back; node disconnect cleans pending child receivers; two-hop path covered by composition — gateway-side routing/storage/receiver tests + node-side `GatewayClient` and storage delivery tests (capacity release on the worker via `AdmissionScope`, storage erasure on node A).

## 4. Build and verification

- [x] 4.1 Update BUILD.bazel targets/deps for the new nodeagent core components (incl. `default_local_scheduler_lib`, `alwayslink`, linked into the `nodeagent` binary), the probe/push test conversions, the workflow example handler, and the gateway receiver/router changes.
- [x] 4.2 Run `make build` and `make test`; fix failures; run `make check`.
- [x] 4.3 Run `openspec validate` on the change; confirm spec deltas apply cleanly against main specs.
- [x] 4.4 Update `AGENTS.md` architecture notes: construction-injected child submitter, child router (submission + frame demux), bundled `default` scheduler owning the storage and child-outcome frames, push/probe Unimplemented `Schedule`, `GatewayClient`, gateway upstream-child routing, and the "interface unchanged" constraint.