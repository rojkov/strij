# Tasks

## 1. libjq build wiring

- [ ] 1.1 Spike: add `http_archive(name = "jq", sha256 = "71b8d6e8f5fe81f6c6d0d110e3892251f6ce76ed095abd315e26e6e1193af3af", strip_prefix = "jq-jq-1.8.2")` to root `MODULE.bazel` and `configure_make(name = "libjq", configure_in_place = True, lib_source = "@jq//:all_srcs", out_static_libs = ["libjq.a"], args = ["--disable-maintainer-mode", "--with-oniguruma=builtin"], visibility = ["//visibility:public"])` to root `BUILD.bazel`, then verify `bazel build //:libjq` succeeds, that a probe target can `#include "jq.h"` and `#include "jv.h"` and link (add oniguruma/decNumber artifacts to `out_static_libs` if they surface separately), and that no bison/flex/automake regeneration is triggered (build log shows no `autoheader`/`bison` invocations)
- [ ] 1.2 Verify the vendored C deps documentation reflects jq: confirm `test/consumer` needs no redeclaration by running `make build_consumer` and `make test_consumer` unchanged, and note the outcome in `docs/extension-author-guide.md` line 123 list if the framework graph ever pulls `//:libjq`

## 2. Jv RAII wrapper

- [ ] 2.1 Implement `strij::utils::Jv` as a move-only RAII class in `src/common/core/utils/jv.hh` (owns a `jv`, frees it in the destructor, records copy via `jv_copy`, exposes `Parse(text)` -> `Jv` and a ToJSON-text dump helper backed by `jv_parse`/`jv_dumpf`), packaged as `strij_cc_library(name = "jv_lib", ...)` with dep `//:libjq`, and verify `bazel build //src/common/core/utils:jv_lib` compiles and `make check_includes` / `make check_namespaces` still pass (wrapper must not appear under `include/`)
- [ ] 2.2 Write `test/common/core/utils/jv_test.cc` covering: construction from valid JSON text and round-trip back to equivalent JSON text; parse failure surfaces as an error (no throw out of the wrapper boundary, no leak); move-only semantics (copy deleted, move transfers ownership, double-free is impossible); refcount safety by copying a `Jv` and destroying both copies, and verify `bazel test //test/common/core/utils:jv_test` passes with ASan (`make test_asan`) to confirm no leaks/double-frees

## 3. Evaluator interface, factory, deps

- [ ] 3.1 Define the abstract `strij::extensions::Evaluator` (pure `Run(const Jv&, std::span<const Jv> args) -> absl::StatusOr<std::vector<Jv>>`), `EvaluatorPtr`, pure `EvaluatorFactory` (`Name()`, `CreateEmptyConfigProto()`, `Compile(const Message&, std::string_view source, std::vector<std::string> variable_names, const EvaluatorDeps&) -> absl::StatusOr<EvaluatorPtr>`), and empty `EvaluatorDeps` struct in `src/common/extensions/evaluators/evaluator.hh` (Rule of Five, deleted copies, matching the scheduler/fetcher factory shapes), packaged as `strij_cc_library(name = "evaluator_interface_lib", ...)` under `src/common/extensions/evaluators/BUILD.bazel`, and verify `bazel build //src/common/extensions/evaluators:evaluator_interface_lib` compiles and coherence check passes (namespace `strij::extensions::evaluators` under a common dir)
- [ ] 3.2 Add a trivial in-repo test `test/common/extensions/evaluators/evaluator_interface_test.cc` with a fake `EvaluatorFactory` subclass proving the contract is implementable (register via `REGISTER_FACTORY`, `Compile` returns a fake `Evaluator`, `Run` returns values), and verify `bazel test //test/common/extensions/evaluators:evaluator_interface_test` passes

## 4. JqEvaluator implementation and config proto

- [ ] 4.1 Declare `api/common/extensions/evaluators/jq/jq.proto` with `package strij.extensions.evaluators.jq` and an empty (for now) `JqEvaluatorConfig` message; add `proto_library` + `cc_proto_library` (`jq_cc_proto`) in a BUILD next to it, and verify `bazel build //api/common/extensions/evaluators/jq:jq_cc_proto` compiles
- [ ] 4.2 Implement `JqEvaluator` + `JqEvaluatorFactory` in `src/common/extensions/evaluators/jq/jq_evaluator.{hh,cc}`: `Name() == "jq"`, `CreateEmptyConfigProto()` returns `JqEvaluatorConfig`, `Compile` uses `jq_state_create` + `jq_compile_args` (bound to the provided `variable_names`; syntax error surfaces via `jq_get_error_message` as a non-OK status), `Run` uses `jq_start_args`/`jq_next` collecting **all** outputs as `vector<Jv>` (error from jq returned as status, program left reusable), packaged as `strij_cc_library(name = "jq_evaluator_lib", alwayslink = True, ...)` with the factory `REGISTER_FACTORY`ed in the `.cc` and deps on `jv_lib`, `jq_cc_proto`, evaluator interface, absl status/statusor, and `//:libjq`; verify `bazel build //src/common/extensions/evaluators/jq:jq_evaluator_lib` compiles and `make check_namespaces` passes
- [ ] 4.3 Write `test/common/extensions/evaluators/jq/jq_evaluator_test.cc` covering: faithful reference semantics (e.g. `.name`, `map(.x)`, `length`, string interpolation `"\(.a)"` against a real input matching `jq`'s documented behavior); named-variable binding via `jq_compile_args` (expression referencing a bound variable returns the correct value and an unbound variable is a compile error); invalid source V compile error carries jq's message; runtime type error returns a failing status and does not corrupt subsequent runs; multi-output run returns all outputs; JSON round-trip via `Jv` works end to end; register the eval factory in the test and resolve it through the `Registry` by `"jq"`; verify `bazel test //test/common/extensions/evaluators/jq:jq_evaluator_test` passes (plus `make test_asan` for the whole `//src/...` breadth)

## 5. Both-sides loader

- [ ] 5.1 Implement `strij::loaders::CreateEvaluator(config, deps, source, variable_names) -> absl::StatusOr<EvaluatorPtr>` in `src/common/loaders/evaluator_loader.{hh,cc}` (look up `config.name()` in `Registry<EvaluatorFactory>`, unpack or tolerate absence of `typed_config` into `CreateEmptyConfigProto`, forward to `Compile`, return `absl::NotFoundError` for unregistered names), packaged as `strij_cc_library(name = "evaluator_loader_lib", ...)` in `src/common/loaders/BUILD.bazel`, and verify `bazel build //src/common/loaders:evaluator_loader_lib` compiles and coherence/includes checks pass (common-dir file opening bare `strij::loaders`)
- [ ] 5.2 Write `test/common/loaders/evaluator_loader_test.cc` covering: config with an unregistered name -> `NotFound`; config naming the registered `"jq"` factory with no `typed_config` -> succeeds and runs an expression; config with a `typed_config` wrapping a `JqEvaluatorConfig` -> unpacks and runs; scoping of the load to resolves a compiled `EvaluatorPtr` that survives the loader call; verify `bazel test //test/common/loaders:evaluator_loader_test` passes

## 6. Docs, checks, and full verification

- [ ] 6.1 Update `docs/extension-author-guide.md`: add `loaders` to the shared row of the directory table (`src/common/{core,extensions,loaders}/`), add `strij::loaders` to the namespace doctrine list, and add `jq` to the vendored C deps list (line 123) with a note that unlike liburing/llhttp/yaml_cpp consumers do not need to redeclare it today; verify the rendered markdown is coherent (grep the doc for `strij::loaders` and `loaders`)
- [ ] 6.2 Update `AGENTS.md`: refresh the "External deps" and "Root BUILD.bazel" bullets for jq 1.8.2, add `src/common/loaders/` to the layout paragraph, add `strij::loaders` to the namespace doctrine line, and note the include-purity reason the evaluator category stays `src/`-internal; verify fact-check markers (`make check` unaffected)
- [ ] 6.3 Run the full gate: `make check` (includes + namespaces), `make build`, `make test`, and `make test_asan` on `//src/...` — all green with no new violations; verify `make build_consumer` / `make test_consumer` still pass (no jq redeclaration needed per D7)